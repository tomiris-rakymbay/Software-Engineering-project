import 'package:flutter/material.dart';

class SalesRequestsPage extends StatelessWidget {
  const SalesRequestsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Incoming Requests", style: TextStyle(fontSize: 22)),
    );
  }
}
